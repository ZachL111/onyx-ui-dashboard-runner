# Review Journal

This journal records the domain cases that matter before widening the public API.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its frontend apps focus without claiming live deployment or external usage.

## Cases

- `baseline`: `view drift`, score 117, lane `watch`
- `stress`: `state pressure`, score 114, lane `watch`
- `edge`: `layout risk`, score 233, lane `ship`
- `recovery`: `interaction cost`, score 220, lane `ship`
- `stale`: `view drift`, score 143, lane `ship`

## Note

This file is intentionally plain so the fixture remains the source of truth.
